runners:
  teensy_loader_cli:
    command: teensy_loader_cli
    flash-args:
      - --mcu=TEENSY41
      - -v
      - -w
      - ${HEX_FILE}
    reset:
      command: teensy_reboot
      requires:
        - teensy-tools